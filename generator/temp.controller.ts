import { <%= IName %> } from '@controllers/types';
import { injectable } from 'inversify';
import 'reflect-metadata';

injectable();
export class <%= upCaseName %>Controller implements <%= IName %> {

}
