import { <%= serviceName %>Service } from '@/services';
import 'jest';

describe('<%= serviceName %> Service', () => {

  it('', () => {
    // Add your unit tests
    expect(true).toBe(true);
  });
});
