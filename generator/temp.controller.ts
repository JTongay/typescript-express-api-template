import { <%= IName %> } from '@controllers/types';
import { injectable } from 'inversify';
import 'reflect-metadata';

injectable();
export default class <%= upCaseName %>Controller implements <%= IName %> {

}
